# GENERATED FILE -- do not edit. Source: tools/recipes/transport_ship.toml
# Player 1: Blue #0043D6 (linear 0.0, 0.056128, 0.672443)
# Regenerate: python tools/gen_player_colour_recipes.py

# Transport ship, Dock from age 2 (PLAN.md 9.2).
#
# Corrected 2026-08-15 from `structures/celts/rowboat.xml` to the SKIFF. Both are
# Celtic, so the "units are Celtic" error (see knight.toml) did not cause this
# one -- it was picked by eye, "smallest hull suits a troop ferry", without
# resolving the owner's pick. Resolved, it is:
#
#   units/gaul/ship_scout  ->  simulation/templates/units/gaul/ship_scout.xml
#     -> <VisualActor><Actor>structures/celts/skiff.xml</Actor>
#
# Lesson: guessing from a plausible description landed on the wrong one of two
# similar actors, and looked right enough that nothing questioned it. Resolve
# the template even when the guess is confident.
id      = "vis.transport_ship.blue"
adapter = "zeroad"

[source]
# Player 1 (Blue, #0043D6). Generated by
# tools/gen_player_colour_recipes.py -- edit that, not this.
player_colour = [0.0, 0.056128, 0.672443]
actor = "structures/celts/skiff.xml"

[render]
# Measured on the rowboat; the skiff is a different hull, so treat this as a
# starting guess and read the clip report on the first bake.
canvas = [448, 448]
supersample = 2
directions = 8
frame_origin_at = 0.2
engine = "eevee"
samples = 24
# The zeroad adapter renders every subject 180 deg from the direction the
# atlas then labels it; this cancels it. Added 2026-08-25 -- asset_request.md P2.
yaw_offset_deg = 180.0

[attribution]
source = "0 A.D. (Wildfire Games)"
licence = "CC-BY-SA 3.0"
licence_url = "http://creativecommons.org/licenses/by-sa/3.0/"
author = "Wildfire Games"
author_url = "http://www.wildfiregames.com/"
actor = "art/actors/structures/celts/skiff.xml"
