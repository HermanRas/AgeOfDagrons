# GENERATED FILE -- do not edit. Source: tools/recipes/cavalry_archer.toml
# Player 1: Blue #0043D6 (linear 0.0, 0.056128, 0.672443)
# Regenerate: python tools/gen_player_colour_recipes.py

# Cavalry archer -- and it throws a javelin, because the Celts have no bows at
# all (PLAN.md 9.2.1 item 8). The `_e` elite tier of the Gaulish mounted
# javelinist, so it reads as a step above vis.scout_cavalry rather than as the
# same unit twice.
#
# See knight.toml for the composite-unit machinery this depends on.
id      = "vis.cavalry_archer.blue"
adapter = "zeroad"

[source]
# Player 1 (Blue, #0043D6). Generated by
# tools/gen_player_colour_recipes.py -- edit that, not this.
variant_seed = "vis.cavalry_archer"
player_colour = [0.0, 0.056128, 0.672443]
actor = "units/achaemenids/cavalry_archer_b_m.xml"

[render]
canvas = [384, 448]
supersample = 2
directions = 8
frame_origin_at = 0.2
engine = "eevee"
samples = 24
# The zeroad adapter renders every subject 180 deg from the direction the
# atlas then labels it; this cancels it. Added 2026-08-25 -- asset_request.md P2.
yaw_offset_deg = 180.0

[anims.idle]
clip = "Idle"
frames = 12
fps = 8
loop = true

[anims.walk]
clip = "Walk"
frames = 12
fps = 15
loop = true

[anims.attack]
clip = "attack_ranged"
frames = 12
fps = 15
loop = true

[anims.die]
clip = "death"
frames = 10
fps = 12
loop = false

[anims.decay]
clip = "death"
frames = 2
fps = 1
loop = false
# Pinned to the clip's final pose 2026-08-16. Without this, decay's first
# frame is Death at t=0 -- the unit still standing -- so the corpse stood up
# for one frame before falling over again. See isobake AnimSpec.start.
start = 1.0

[attribution]
source = "0 A.D. (Wildfire Games)"
licence = "CC-BY-SA 3.0"
licence_url = "http://creativecommons.org/licenses/by-sa/3.0/"
author = "Wildfire Games"
author_url = "http://www.wildfiregames.com/"
actor = "art/actors/units/achaemenids/cavalry_archer_b_m.xml"
