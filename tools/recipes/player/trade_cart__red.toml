# GENERATED FILE -- do not edit. Source: tools/recipes/trade_cart.toml
# Player 2: Red #D50032 (linear 0.665387, 0.0, 0.031896)
# Regenerate: python tools/gen_player_colour_recipes.py

# Trade cart, from 0 A.D.'s Athenian "Trader Cart" -- ASSET_MISSING.md 2.1.
#
# BLOCKED, 2026-08-08, identical root cause to trebuchet_deployed.toml: the
# cart carries a horse (`units/hellenes/trader_h.xml`) and a rider
# (`trader_r.xml`) as nested props, each with their own armature, and
# isobake's `next(o for o in imported if o.type == "ARMATURE")` grabs one of
# theirs instead of the cart's own mechanical rig (`skeletal/trader_cart.dae`)
# -- same failure, same "Available: []", confirming this is a systemic
# pattern (any actor with an animated nested-prop sub-actor), not one-off.
# Not re-attempting the armature-picking fix here -- already tried and
# reverted on the trebuchet for the same underlying issue; see that recipe's
# note for why "most bones" and "first non-broken" both failed.
id      = "vis.trade_cart.red"
adapter = "zeroad"

[source]
# Player 2 (Red, #D50032). Generated by
# tools/gen_player_colour_recipes.py -- edit that, not this.
variant_seed = "vis.trade_cart"
player_colour = [0.665387, 0.0, 0.031896]
actor = "units/athenians/trader.xml"
# UNBLOCKED 2026-08-14, same fix as trebuchet_deployed.toml: `_subject_armature`
# now identifies the cart's own "TwoWheeler" rig rather than one of the four
# nested-prop skeletons (draft horse, driver) that import alongside it, by asking
# which armature nothing else anchors with a COPY_LOCATION/COPY_ROTATION
# constraint. All four props then get their own clips via `_prop_actor_animations`
# -- without that the cart rolled with a frozen driver, which is exactly how this
# was first spotted as still wrong.

[render]
canvas = [320, 320]
supersample = 2
directions = 5
frame_origin_at = 0.2
engine = "eevee"
samples = 24
# The zeroad adapter renders every subject 180 deg from the direction the
# atlas then labels it; this cancels it. Added 2026-08-25 -- asset_request.md P2.
yaw_offset_deg = 180.0

[anims.idle]
clip = "Idle"
frames = 12
fps = 8
loop = true

[anims.walk]
clip = "Walk"
frames = 12
fps = 15
loop = true

[attribution]
source = "0 A.D. (Wildfire Games)"
licence = "CC-BY-SA 3.0"
licence_url = "http://creativecommons.org/licenses/by-sa/3.0/"
author = "Wildfire Games"
author_url = "http://www.wildfiregames.com/"
actor = "art/actors/units/athenians/trader.xml"
