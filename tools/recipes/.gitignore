# Throwaway diagnostic recipes: one direction, clips stripped, written by the
# probe scripts to answer a single question cheaply (does this actor take the
# tint, did the corpse stop standing up) before committing to a multi-hour
# batch. Regenerated on demand and never part of a real bake.
probe/