# Wolf, from 0 A.D.'s fauna wolf -- hostile wildlife (ASSET_MISSING.md 2.3).
# STATIC, same reasoning as deer.toml / boar.toml: a quadruped with its own
# armature (32 bones) -- no clip attached, so the quadruped
# animation-transfer bug (PLAN.md 13.2 item 8) never triggers.
id      = "vis.wolf"
adapter = "zeroad"

[source]
actor = "fauna/wolf.xml"

[render]
# `isobake inspect` measures 0.800 x 3.410 x 2.291 m raw -- close to deer.xml,
# slightly taller/leaner, so deer.toml's canvas is a safe start.
canvas = [128, 128]
supersample = 4
directions = 5
frame_origin_at = 0.15
engine = "eevee"
samples = 24

[attribution]
source = "0 A.D. (Wildfire Games)"
licence = "CC-BY-SA 3.0"
licence_url = "http://creativecommons.org/licenses/by-sa/3.0/"
author = "Wildfire Games"
author_url = "http://www.wildfiregames.com/"
actor = "art/actors/fauna/wolf.xml"
